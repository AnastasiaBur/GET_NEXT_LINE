lstnew
lstadd
strnew +	char* (size) - returns a string initialized to \0
strjoin ++	char* (*pre,*post) - returns a string pre + post ending with \0
strchr ++	char* (*src, char) - locate character in a string
strlen		size_t (*src) - find length of a string
strclr		void (*src) - set every char of a string to \0
strsub +	char* (*src, starting i, size) - returns a substring 
strdup +	char* (*src) save a copy of a string
strdel		void (**src) takes the adr of a str and sets it's pointer to NULL
memalloc
